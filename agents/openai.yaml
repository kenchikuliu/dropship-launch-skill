interface:
  display_name: "Dropship Launch"
  short_description: "Market-aware Shopify test workflow."
  default_prompt: "Use $dropship-launch to choose a launch market, pick a test product, draft the product page, and build a 7-day Meta ads validation plan."

policy:
  allow_implicit_invocation: true
